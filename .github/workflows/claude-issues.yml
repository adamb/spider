name: Claude Code - Fix Issues

on:
  issues:
    types: [opened, labeled]
  issue_comment:
    types: [created]

jobs:
  fix-issue:
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'issues') ||
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude'))
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.OLLAMA_API_KEY }}
          show_full_output: true    
          prompt: |
            An issue was filed: "${{ github.event.issue.title }}"

            ${{ github.event.issue.body }}

            Analyze this issue, implement a fix, and open a pull request.
          claude_args: "--model kimi-k2.7-code:cloud --max-turns 20 --context-window 256000"
        env:
          ANTHROPIC_BASE_URL: https://ollama.com/api
          ANTHROPIC_AUTH_TOKEN: ${{ secrets.OLLAMA_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.OLLAMA_API_KEY }}

